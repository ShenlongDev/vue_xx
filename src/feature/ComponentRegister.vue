<template>
  <h1>组件注册</h1>
  <p>
    一个 Vue 组件在使用前需要先被 “注册”，这样 Vue 才能在渲染模板时找到其对应的实现。
    组件注册用两种方式：全局注册和局部注册。
  </p>

  <h2>全局注册</h2>
  <p>我们可以使用 Vue 应用实例的 .component() 方法，让组件在当前 Vue 应用中全局可用。</p>
  <p>如果使用单文件组件，你可以注册被导入的 .vue 文件。</p>
  <p>.component() 方法，可以被链式调用：</p>
  <p>全局注册的组件可以在此应用的任意组件的模板中使用：</p>
  <p>所有的子组件也可以使用全局注册的组件，这意味着这三个组件也都可以在彼此内部使用。</p>
</template>
